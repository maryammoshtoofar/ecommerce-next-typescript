import React from 'react';

type Props = {};

const Dashboard = (props: Props) => {
  return (
    <section className="col-span-12 sm:col-span-9 row-start-2 w-full ">
      ADMIN DASHBOARD
    </section>
  );
};

export default Dashboard;
